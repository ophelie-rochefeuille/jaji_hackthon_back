<form method="post" action="{{ path('app_soignant_delete', {'id': soignant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ soignant.id) }}">
    <button class="btn">Delete</button>
</form>
